import TVShowsContainer from "../components/containers/TVShowsContainer";

const TVShowsScreen = ({ navigation, route }) => (
  <TVShowsContainer navigation={navigation} route={route} />
);

export default TVShowsScreen;
