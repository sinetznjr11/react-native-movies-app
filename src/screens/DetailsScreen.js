import DetailsContainer from "../components/containers/DetailsContainer";

const DetailsScreen = ({ navigation, route }) => (
  <DetailsContainer navigation={navigation} route={route} />
);

export default DetailsScreen;
